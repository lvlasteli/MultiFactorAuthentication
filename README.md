To use these three command bellow, it is expected to already have create database in PostgreSQL.
Environment variables needed to be specified.

To create models
`npm run create`

To fill DummyData
`npm run populate`

Combination
`npm run create_populate`

Run server
`npm run server`

Run client
`npm run client`